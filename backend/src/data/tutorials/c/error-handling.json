{
  "tag": "error-handling",
  "language": "c",
  "title": "C Error Handling",
  "category": "C Errors",
  "categoryOrder": 7,
  "subCategory": null,
  "subCategoryOrder": 0,
  "order": 4,
  "sections": [
    {
      "heading": "Error Handling in C",
      "blocks": [
        {
          "type": "text",
          "content": "Error handling lets you detect and respond to problems in your program, like a file that can't be opened or memory that can't be allocated, so your program doesn't crash or behave unexpectedly."
        },
        {
          "type": "text",
          "content": "Unlike some languages, C does not have built-in exception handling (like **try/catch**). Instead, C uses return values, global error codes, and helper functions like `perror()` and `strerror()`."
        }
      ]
    },
    {
      "heading": "Using Return Values",
      "blocks": [
        {
          "type": "text",
          "content": "In the previous chapter, you learned that functions like `fopen()` return `NULL` when something goes wrong."
        },
        {
          "type": "text",
          "content": "You can check for `NULL` using an `if` statement to detect and handle errors before your program crashes."
        },
        {
          "type": "text",
          "content": "In the example below, we try to open a file that does not exist. Since `fopen()` fails, it returns `NULL` and we print an error message:"
        },
        {
          "type": "text",
          "content": "Result: `Error opening file.` Get More Details If you want more details about what went wrong, you can use the `perror()` function."
        },
        {
          "type": "text",
          "content": "It prints a custom error message followed by a description of the last error that occurred:"
        },
        {
          "type": "text",
          "content": "Result: `Error opening file: No such file or directory` Using strerror() and errno `errno` is a global variable that stores the error code from the last failed operation. You can include `<errno.h>` to access it, and `strerror(errno)` will convert the error code into a readable message:"
        },
        {
          "type": "text",
          "content": "Result: `Error: No such file or directory` Common Error Codes Error constants are defined in `<errno.h>`. You can compare `errno` to them to detect specific issues:"
        },
        {
          "type": "text",
          "content": "Result: `The file was not found.` Using exit() to Stop the Program If you want to stop the program immediately when an error occurs, you can use `exit()`. It lets you return a status code to the operating system."
        },
        {
          "type": "text",
          "content": "Exit codes help signal whether the program finished successfully or with an error, like:"
        },
        {
          "type": "list",
          "items": [
            "`0` means success",
            "Non-zero values (like `1` or `EXIT_FAILURE`) indicate errors"
          ]
        },
        {
          "type": "text",
          "content": "Result: `Failed to open file.` Common Exit Status Codes CodeMeaning 0Success - the program completed normally 1Error - something went wrong EXIT_SUCCESSSame as 0 (defined in `<stdlib.h>`) EXIT_FAILURESame as a non-zero error code (also in `<stdlib.h>`) **Tip:** You can use `EXIT_SUCCESS` and `EXIT_FAILURE` instead of numbers to make your code more readable."
        },
        {
          "type": "text",
          "content": "Result: `Could not open nothing.txt: No such file or directory` Summary Many C functions return `NULL` when something goes wrong Use `perror()` to print a message about the error Use `strerror(errno)` to get the error message as a string `errno` stores the error code from the last failed action You can compare `errno` to values like `ENOENT` (file not found) or `ENOMEM` (not enough memory) Use `exit()` to stop the program early if there's an error **Tip:** Always check for errors after file operations, memory allocation, and system calls. Ignoring errors can lead to unexpected behavior or crashes."
        }
      ]
    }
  ]
}
