{
  "tag": "files-create",
  "language": "c",
  "title": "C Create Files",
  "category": "C Files",
  "categoryOrder": 3,
  "subCategory": null,
  "subCategoryOrder": 0,
  "order": 1,
  "sections": [
    {
      "heading": "File Handling",
      "blocks": [
        {
          "type": "text",
          "content": "In C, you can create, open, read, and write to files by declaring a pointer of type `FILE`, and use the `fopen()` function:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\nfptr = fopen(filename, mode);"
        },
        {
          "type": "text",
          "content": "`FILE` is basically a data type, and we need to create a pointer variable to work with it (`fptr`). For now, this line is not important. It's just something you need when working with files."
        },
        {
          "type": "text",
          "content": "To actually open a file, use the `fopen()` function, which takes two parameters:"
        },
        {
          "type": "text",
          "content": "`w` - Writes to a file `a` - Appends new data to a file `r` - Reads from a file"
        }
      ]
    },
    {
      "heading": "Create a File",
      "blocks": [
        {
          "type": "text",
          "content": "To create a file, you can use the `w` mode inside the `fopen()` function."
        },
        {
          "type": "text",
          "content": "The `w` mode is used to write to a file. **However**, if the file does not exist, it will create one for you:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Create a file\nfptr = fopen(\"filename.txt\", \"w\");\n\n// Close the file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "**Note:** The file is created in the same directory as your other C files, if nothing else is specified."
        },
        {
          "type": "text",
          "content": "On our computer, it looks like this:"
        },
        {
          "type": "text",
          "content": "**Tip:** If you want to create the file in a specific folder, just provide an absolute path (remember to use double backslashes to create a single backslash (`\\`), like we specified in strings special characters):"
        },
        {
          "type": "code",
          "content": "    fptr = fopen(\"C:\\\\directoryname\\\\filename.txt\", \"w\");"
        },
        {
          "type": "text",
          "content": "Did you notice the `fclose()` function in our example above?"
        },
        {
          "type": "text",
          "content": "This will close the file when we are done with it."
        },
        {
          "type": "text",
          "content": "It is considered as good practice, because it makes sure that:"
        },
        {
          "type": "list",
          "items": [
            "Changes are saved properly",
            "Other programs can use the file (if you want)",
            "Clean up unnecessary memory space"
          ]
        },
        {
          "type": "text",
          "content": "In the next chapters, you will learn how to write content to a file and read from it."
        }
      ]
    }
  ]
}
