{
  "tag": "files-read",
  "language": "c",
  "title": "C Read Files",
  "category": "C Files",
  "categoryOrder": 3,
  "subCategory": null,
  "subCategoryOrder": 0,
  "order": 3,
  "sections": [
    {
      "heading": "Read a File",
      "blocks": [
        {
          "type": "text",
          "content": "In the previous chapter, we wrote to a file using `w` and `a` modes inside the `fopen()` function."
        },
        {
          "type": "text",
          "content": "To **read** from a file, you can use the `r` mode:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = fopen(\"filename.txt\", \"r\");"
        },
        {
          "type": "text",
          "content": "This will make the `filename.txt` opened for reading."
        },
        {
          "type": "text",
          "content": "It requires a little bit of work to read a file in C. Hang in there! We will guide you step-by-step."
        },
        {
          "type": "text",
          "content": "Next, we need to create a string that should be big enough to store the content of the file."
        },
        {
          "type": "text",
          "content": "For example, let's create a string that can store up to 100 characters:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = fopen(\"filename.txt\", \"r\");\n\n// Store the content of the file\nchar myString[100];"
        },
        {
          "type": "text",
          "content": "In order to read the content of `filename.txt`, we can use the `fgets()` function."
        },
        {
          "type": "text",
          "content": "The `fgets()` function takes three parameters:"
        },
        {
          "type": "code",
          "content": "    fgets(myString, 100, \n    fptr);"
        },
        {
          "type": "list",
          "items": [
            "The first parameter specifies where to store the file content, which will be in the `myString` array we just created.",
            "The second parameter specifies the maximum size of data to read, which should match the size of `myString` (`100`).",
            "The third parameter requires a file pointer that is used to read the file (`fptr` in our example)."
          ]
        },
        {
          "type": "text",
          "content": "Now, we can print the string, which will output the content of the file:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = fopen(\"filename.txt\", \"r\");\n\n// Store the content of the file\nchar myString[100];\n\n// Read \n    the content and store it inside myString\nfgets(myString, 100, fptr);\n\n// Print the file content\nprintf(\"%s\", myString);\n\n// Close the \n    file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "`Hello World!`"
        },
        {
          "type": "text",
          "content": "**Note:** The `fgets` function only reads the first line of the file. If you remember, there were two lines of text in `filename.txt`."
        },
        {
          "type": "text",
          "content": "To read every line of the file, you can use a `while` loop:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = \n    fopen(\"filename.txt\", \"r\");\n\n// Store the content of the file\nchar \n    myString[100];\n\n// Read the content and print it\n\n    while(fgets(myString, 100, fptr)) {\n  printf(\"%s\", myString);\n}\n\n    // Close the file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "`Hello World! Hi everybody!`"
        }
      ]
    },
    {
      "heading": "Good Practice",
      "blocks": [
        {
          "type": "text",
          "content": "If you try to open a file for reading that does not exist, the `fopen()` function will return `NULL`."
        },
        {
          "type": "text",
          "content": "**Tip:** As a good practice, we can use an `if` statement to test for `NULL`, and print some text instead (when the file does not exist):"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = \n    fopen(\"loremipsum.txt\", \"r\");\n\n// Print some text \n    if the file does not exist\nif(fptr == NULL) {\n  printf(\"Not able to open the \n    file.\");\n}\n\n// Close the file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "If the file does not exist, the following text is printed:"
        },
        {
          "type": "text",
          "content": "`Not able to open the file.`"
        },
        {
          "type": "text",
          "content": "With this in mind, we can create a more sustainable code if we use our \"read a file\" example above again:"
        },
        {
          "type": "text",
          "content": "If the file exist, read the content and print it. If the file does not exist, print a message:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in read mode\nfptr = \n    fopen(\"filename.txt\", \"r\");\n\n// Store the content of the file\nchar \n    myString[100];\n\n// If the file exist\nif(fptr != NULL) {\n\n    // Read the content and print it\n  while(fgets(myString, 100, fptr)) \n    {\n    printf(\"%s\", myString);\n  }\n\n// \n    If the file does not exist \n} else {\n  printf(\"Not able to open the file.\");\n}\n\n// Close the file\n\n    fclose(fptr);"
        },
        {
          "type": "text",
          "content": "`Hello World! Hi everybody!`"
        },
        {
          "type": "text",
          "content": "**Note:** You will learn more about `NULL` in our NULL chapter."
        }
      ]
    }
  ]
}
