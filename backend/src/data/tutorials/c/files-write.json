{
  "tag": "files-write",
  "language": "c",
  "title": "C Write To Files",
  "category": "C Files",
  "categoryOrder": 3,
  "subCategory": null,
  "subCategoryOrder": 0,
  "order": 2,
  "sections": [
    {
      "heading": "Write To a File",
      "blocks": [
        {
          "type": "text",
          "content": "Let's use the `w` mode from the previous chapter again, and write something to the file we just created."
        },
        {
          "type": "text",
          "content": "The `w` mode means that the file is opened for **writing**. To insert content to it, you can use the `fprintf()` function and add the pointer variable (`fptr` in our example) and some text:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in writing mode\nfptr = fopen(\"filename.txt\", \"w\");\n\n// Write some text to the file\nfprintf(fptr, \"Some text\");\n\n// Close the file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "As a result, when we open the file on our computer, it looks like this:"
        },
        {
          "type": "text",
          "content": "**Note:** If you write to a file that already exists, the old content is deleted, and the new content is inserted. This is important to know, as you might accidentally erase existing content."
        },
        {
          "type": "text",
          "content": "For example:"
        },
        {
          "type": "code",
          "content": "    fprintf(fptr, \"Hello \n    World!\");"
        },
        {
          "type": "text",
          "content": "As a result, when we open the file on our computer, it says \"Hello World!\" instead of \"Some text\":"
        }
      ]
    },
    {
      "heading": "Append Content To a File",
      "blocks": [
        {
          "type": "text",
          "content": "If you want to add content to a file without deleting the old content, you can use the `a` mode."
        },
        {
          "type": "text",
          "content": "The `a` mode appends content at the end of the file:"
        },
        {
          "type": "code",
          "content": "    FILE *fptr;\n\n// Open a file in append mode\nfptr = fopen(\"filename.txt\", \"a\");\n\n// Append some text to the file\nfprintf(fptr, \"\\nHi everybody!\");\n\n// Close the file\nfclose(fptr);"
        },
        {
          "type": "text",
          "content": "As a result, when we open the file on our computer, it looks like this:"
        },
        {
          "type": "text",
          "content": "**Note:** Just like with the `w` mode; if the file does not exist, the `a` mode will create a new file with the \"appended\" content."
        }
      ]
    }
  ]
}
