{
  "tag": "nested-if",
  "language": "c",
  "title": "C Nested If",
  "category": "C Tutorial",
  "categoryOrder": 1,
  "subCategory": "If...Else",
  "subCategoryOrder": 7,
  "order": 5,
  "sections": [
    {
      "heading": "Nested If",
      "blocks": [
        {
          "type": "text",
          "content": "You can also place an `if` statement inside another `if`. This is called a **nested if** statement. A nested `if` lets you check for a condition only if another condition is already `true`."
        },
        {
          "type": "syntax",
          "content": "if (condition1) {\n  // code to run if condition1 is true\n  if (condition2) {\n    // code to run if both condition1 and condition2 are true\n  }\n}"
        }
      ]
    },
    {
      "heading": "Example",
      "blocks": [
        {
          "type": "text",
          "content": "In this example, we first check if `x` is greater than `10`. If it is, we then check if `y` is greater than `20`:"
        },
        {
          "type": "code",
          "content": "int x = 15;\nint y = 25;\n\nif (x > 10) {\n  printf(\"x is greater than 10\\n\");\n\n  // Nested if\n  if (y > 20) {\n    printf(\"y is also greater than 20\\n\");\n  }\n}"
        },
        {
          "type": "text",
          "content": "**Result:**\n\n```\nx is greater than 10\ny is also greater than 20\n```"
        }
      ]
    },
    {
      "heading": "Real-Life Example",
      "blocks": [
        {
          "type": "text",
          "content": "Nested `if` statements are useful when you need to test multiple conditions that depend on each other. For example, checking if a person is old enough to vote, and if they are a citizen:"
        },
        {
          "type": "code",
          "content": "int age = 20;\nbool isCitizen = true;\n\nif (age >= 18) {\n  printf(\"Old enough to vote.\\n\");\n\n  if (isCitizen) {\n    printf(\"And you are a citizen, so you can vote!\\n\");\n  } else {\n    printf(\"But you must be a citizen to vote.\\n\");\n  }\n} else {\n  printf(\"Not old enough to vote.\\n\");\n}"
        },
        {
          "type": "text",
          "content": "**Result:**\n\n```\nOld enough to vote.\nAnd you are a citizen, so you can vote!\n```"
        }
      ]
    },
    {
      "heading": "Notes",
      "blocks": [
        {
          "type": "list",
          "items": [
            "You can nest as many `if` statements as you want, but avoid making the code too deep — it can become hard to read.",
            "Nested `if` is often used together with `else` and `else if` for more complex decision making."
          ]
        }
      ]
    }
  ]
}
