{
  "tag": "recursion",
  "language": "c",
  "title": "C Recursion",
  "order": 22,
  "sections": [
    {
      "heading": "Recursion",
      "body": "Recursion is the technique of making a function call itself. This technique provides a way to break complicated problems down into simple problems which are easier to solve. Recursion may be a bit difficult to understand. The best way to figure out how it works is to experiment with it.",
      "code": ""
    },
    {
      "heading": "Recursion Example",
      "body": "Adding two numbers together is easy to do, but adding a range of numbers is more complicated. In the following example, recursion is used to add a range of numbers together by breaking it down into the simple task of adding two numbers:",
      "code": ""
    },
    {
      "heading": "Example Explained",
      "body": "When the sum() function is called, it adds parameter k to the sum of all numbers smaller than k and returns the result. When k becomes 0, the function just returns 0. When running, the program follows these steps: Since the function does not call itself when k is 0, the program stops there and returns the result. The developer should be very careful with recursion as it can be quite easy to slip into writing a function which never terminates, or one that uses excess amounts of memory or processor power. However, when written correctly, recursion can be a very efficient and mathematically-elegant approach to programming.",
      "code": ""
    },
    {
      "heading": "Calculate Factorial with Recursion",
      "body": "This example uses a recursive function to calculate the factorial of 5:",
      "code": ""
    },
    {
      "heading": "Function Pointer",
      "body": "A function pointer is like a normal pointer, but instead of pointing to a variable, it points to a function. This means it stores the address of a function, allowing you to call that function using the pointer. Function pointers let you decide which function to run while the program is running, or when you want to pass a function as an argument to another function. Think of it like saving a phone number - the pointer knows where the function lives in memory, so you can \"call\" it later.",
      "code": ""
    },
    {
      "heading": "Declaring a Function Pointer",
      "body": "The general syntax for declaring a function pointer is: For example: This means ptr is a pointer to a function that takes two int values and returns an int.",
      "code": "returnType (*pointerName)(parameterType1, parameterType2, ...);\n\nint (*ptr)(int, int);"
    },
    {
      "heading": "Assigning a Function to a Pointer",
      "body": "You can assign a function to a pointer in two ways: Both are the same, because the function's name already represents its address in memory.",
      "code": "ptr = add;\n\n  ptr = &add;"
    },
    {
      "heading": "Calling a Function Through a Pointer",
      "body": "Once the pointer is assigned, you can call the function in two ways: Both are valid and do the same thing.",
      "code": "ptr(5, 3);\n\n  (*ptr)(5, 3);"
    },
    {
      "heading": "Function Pointer Example",
      "body": "Now that you know how to declare and assign a function pointer, let's see a complete example:",
      "code": ""
    },
    {
      "heading": "Passing a Function as an Argument",
      "body": "Function pointers can be passed to other functions - this is called a callback. It allows a function to call another function that you provide as input.",
      "code": ""
    },
    {
      "heading": "Function Pointer Array",
      "body": "You can also store several function pointers in an array, so you can choose which function to run while the program is running. This example runs three different functions using an array of function pointers:",
      "code": ""
    },
    {
      "heading": "Function Pointer vs Normal Function",
      "body": "Normal Function | Function Pointer Called directly by its name | Called using a pointer The function is decided before the program runs | You can choose which function to call while the program is running Good for simple code | Good for flexible and reusable code",
      "code": ""
    },
    {
      "heading": "Summary",
      "body": "• A function pointer stores the address of a function. • You can declare, assign, and call functions through it. • It allows passing functions as arguments to other functions. • Useful for callbacks, menus, and flexible program design.",
      "code": ""
    },
    {
      "heading": "Callback Function",
      "body": "A callback function is a function that is passed as an argument to another function. The receiving function can then call it back (run it) whenever it needs to. This is a powerful way to make your code flexible and reusable - you can decide which function should run, without changing the main logic. In C, callback functions are usually implemented using function pointers.",
      "code": ""
    },
    {
      "heading": "Simple Callback Example",
      "body": "Here's a basic example that shows how one function can \"call back\" another:",
      "code": ""
    },
    {
      "heading": "Callback with Parameters",
      "body": "You can also pass functions that take parameters - just make sure the function pointer type matches:",
      "code": ""
    },
    {
      "heading": "Multiple Callbacks",
      "body": "You can use callbacks to let a function behave differently depending on which function you pass in. This is common in sorting, filtering, and event-handling code.",
      "code": ""
    },
    {
      "heading": "Real-World Example: Using Callbacks in qsort()",
      "body": "Many C standard library functions use callbacks. For example, the qsort() function in <stdlib.h> uses a callback to compare elements while sorting. You provide the comparison function, and qsort() calls it as needed. This will sort the elements:",
      "code": ""
    },
    {
      "heading": "Summary",
      "body": "• A callback function is a function passed as an argument to another function. • It allows one function to call another without knowing its name in advance. • Callbacks make code more flexible and reusable. • They are used in the C Standard Library (like qsort()).",
      "code": ""
    }
  ]
}